# ENV-A (LOCAL-RESEARCH) bağımlılıkları
pandas>=2.2
numpy>=2.0
requests>=2.32
matplotlib>=3.9
xgboost>=3.0
pytest>=8.0

# ENV-B (paper) — izleme/kontrol arayüzü (src/arayuz)
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27          # yalnızca testler (FastAPI TestClient)
